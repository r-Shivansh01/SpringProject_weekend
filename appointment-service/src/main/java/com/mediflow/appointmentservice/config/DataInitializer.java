package com.mediflow.appointmentservice.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DataInitializer {

    // Appointment data should be created through the REST API
    // because Patient Service and Doctor Service validation is required.
}
